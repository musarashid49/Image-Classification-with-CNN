# Pakistani Politicians Image Classification - requirements
# Colab already provides torch/torchvision; pinning these is optional.
# Versions listed are minima known to work; pip install -r will upgrade if needed.

# Deep learning (provided by Colab, listed for local installs)
torch>=2.0.0
torchvision>=0.15.0

# Numerics + data
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
Pillow>=10.0.0

# Plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# Notebook helpers
tqdm>=4.65.0
jupyter>=1.0.0
ipywidgets>=8.0.0
